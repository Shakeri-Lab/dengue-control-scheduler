# Python
__pycache__/
*.py[cod]
.venv/
.ipynb_checkpoints/

# Large climate grids (242 MB + 425 MB): distributed as a Release asset, not in git.
# Place them in the repository root; see README.md.
*.mat
*.zip

# Run outputs (the GUI writes one folder per run; default name below)
Aedes_Aegypti/
results*/

# OS
.DS_Store
Thumbs.db
